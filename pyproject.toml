[project]
name = "a2a-multi-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.119.0",
    "fastapi-mcp>=0.4.0",
    "uvicorn>=0.37.0",
]

[tool.uv.workspace]
members = [
    "hotel-agent",
]
